# mylog

A simple and lightweight Flutter/Dart logging package.

## Features

- Print formatted logs
- Info, warning and error logs
- Easy to use
- Lightweight with no extra setup

## Installation

Add this to your `pubspec.yaml`:

```yaml
dependencies:
  mylog: ^0.0.1